# Self-Improvement & Lessons Log

This file acts as the AI Agent's memory for corrections and best practices.

## Entries

| Date | Issue/Mistake | Root Cause | Rule to Prevent Recurrence |
|------|---------------|------------|-----------------------------|
| 2026-05-18 | `ImportError: attempted relative import with no known parent package` when starting Uvicorn | Running `uvicorn main:app` directly from the `backend/` directory causes `main.py` to be loaded as a top-level script, which breaks its package-relative imports. | Run uvicorn from the monorepo root using `uv run --project backend uvicorn backend.main:app --app-dir .` to ensure the parent package is correctly resolved. |
| 2026-05-18 | Next.js API fetches returning 404 HTML (Unexpected token '<' is not valid JSON) in dev mode | Fetching relative path `/api/...` in Next.js dev mode queries port 3000 instead of port 8000, failing to route to FastAPI. | Add development-only rewrites to `next.config.ts` to proxy all `/api/` paths directly to the backend API origin. |
| 2026-05-22 | New QA Agent implementation lacked full XML prompt directory architecture | The AI implemented `QA Agent` rapidly but bypassed the mandatory 6-file prompt architecture rules (`CONTRACT.md`, `system.md`, etc.). | Always strictly follow `prompts/AGENT.md` rules when bootstrapping a new agent. No exceptions for speed. |
| 2026-05-22 | Missing `AGENT.md` docs inside `backend/agents/` subdirectories | When scaffolding new agents (e.g. observability, security), documentation inside the folder was omitted. | Always create an `AGENT.md` detailing Role, Input, and Output inside every `backend/agents/<name>/` directory upon creation. |
| 2026-05-22 | JobServe scraper extracting volatile session links that broke after minutes | The scraper extracted the `href` attribute which contained a Search Hash ID (`shid`) tied to a temporary web session. | When extracting links from Job boards, ignore query parameters like `shid` and reconstruct permalinks using the DOM element `id` instead. |
| 2026-05-24 | Bypassing EXECUTION-RULES.md due to user "YOLO mode" prompt | The prompt explicitly asked to use "YOLO mode", which led to skipping the workflow updates in `docs/tasks/todo.md` and `docs/tasks/lessons.md`. | The `EXECUTION-RULES.md` is the absolute "constitution". It expressly overrides any generalized "YOLO" instructions. Even in YOLO mode, `todo.md` and `lessons.md` MUST be read and updated. |
| 2026-05-24 | Using raw integer status codes in FastAPI exceptions (e.g., `422` instead of `status.HTTP_422_UNPROCESSABLE_ENTITY`) | Hardcoding integer status codes reduces readability and bypasses FastAPI's built-in standard constants. | Always import and use `fastapi.status` constants for any HTTP status code across the API. |
| 2026-05-24 | Failing to proactively track "YOLO mode" discrepancy fixes | Executed a user request to fix gaps between Jira Epics and the design proposal without immediately documenting the fixes in `todo.md` and `lessons.md` during the same turn. | Even in YOLO mode, immediately record any implemented gaps or discrepancy fixes in `todo.md` and `lessons.md` as part of the implementation workflow. |
| 2026-05-24 | Skipping Task and Learning Rules for JD-E7 in YOLO mode | Bypassed updating `todo.md` and `lessons.md` because the prompt explicitly said "in yolo mode". | Never skip updating `docs/tasks/todo.md` and `docs/tasks/lessons.md` when completing Epics. The YOLO instruction applies to planning overhead, not essential record-keeping. |
| 2026-05-24 | Using `cat << EOF` in terminal instead of proper file writing tool | Attempted to append to a file using terminal `cat` inside a bash command during YOLO mode. | Always prioritize using specific tools like `replace_file_content` or `write_to_file` over generic bash terminal commands for editing files. |
| 2026-05-24 | Invalid python package naming with dashes | Renamed directory names to contain dashes (e.g. `question-answer`) to follow `proposal-v4-structure.md` literally, causing Python syntax errors on import. | Always adapt folder names to standard Python package format (`_` instead of `-`) when resolving proposal designs to actual code. |
| 2026-05-24 | Missing playbook constraints in system prompts | Implemented the Cover Letter Agent prompt without fully incorporating the 6-section playbook requirement specified in the Epic task. | Always cross-reference the Epic description task bullets against the generated prompt logic, ensuring structural requirements (e.g., 6-section playbook) are strictly enforced in XML. |

| 2026-05-24 | Incomplete Security/Orchestrator layers | Initial MVP 1 scaffolding of security and orchestrator agents lacked Temporal workflow definition, OWASP middleware, and DLQ frontend integrations. | Always cross-reference Epic task constraints (e.g. Temporal, extra="forbid") before marking MVP 2 agent tasks as done. |

| 2026-05-24 | Incomplete prompt contract & system.md constraints | Initial YOLO mode update of JD-E10 missed the corresponding prompt modifications in JD-E12 (JD-60, JD-61) and Temporal worker configs. | When implementing an Epic containing orchestrator workflows, verify whether corresponding prompt system texts and deployment configs (like `supervisord.conf`) are fully updated. |
| 2026-05-24 | Missing root `AGENT.md` for infrastructure directory | Implemented Epic 11 Terraform infrastructure without checking if the parent `infrastructure/AGENT.md` existed per the new proposal-v4-structure.md. | When creating files for a new Epic domain, always grep the structural source of truth to ensure no governing `AGENT.md` files are omitted. |
